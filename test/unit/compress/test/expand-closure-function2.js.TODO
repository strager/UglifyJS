(function() {
        function one() {
                alert('one');
        }

        (function() {
                alert(two.toString());

                function two() {
                        one();
                }
        }());
}());
